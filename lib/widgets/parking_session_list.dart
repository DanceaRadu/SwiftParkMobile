import 'package:flutter/cupertino.dart';

class ParkingSessionList extends StatelessWidget {
  const ParkingSessionList({super.key, required this.onPay, required this.parkingSessions});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
